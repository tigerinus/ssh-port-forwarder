{
  "name": "ssh-port-forwarder",
  "version": "0.0.1",
  "description": "A simple service that forwards remote port to local",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ssh"
  ],
  "author": "Tiger Wang",
  "license": "ISC",
  "dependencies": {
    "ssh2": "^0.6.1"
  }
}
